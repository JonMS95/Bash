
    int i;

    printf("String provided by the user: %s\r\n", inputStr);

    printf("Reverted string: ");
    for(i = strlen(inputStr) - 1; i > -1; i--)
    {
        printf("%c", inputStr[i]);
    }
    printf("\r\n");

    return 0;
}